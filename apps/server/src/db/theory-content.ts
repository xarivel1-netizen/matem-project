/**
 * Подробная теория по темам курса. Ключ — точный заголовок параграфа (из плана).
 * Матч по title; скрипт seed-theory сообщает о ненайденных.
 * String.raw: чтобы обратные слеши LaTeX (\frac, \sin, ...) не трактовались как escape.
 * Контент генерируется намеренно (правило 3 CLAUDE.md снято владельцем для теории).
 */
export interface TheoryEntry {
  title: string;
  md: string;
}

export const theoryContent: TheoryEntry[] = [
  {
    title: 'Функции: определение, область определения и значений',
    md: String.raw`# Функция

## Что такое функция

**Функция** — это правило (закон), по которому каждому значению $x$ из некоторого
множества ставится в соответствие **ровно одно** значение $y$. Пишут $y = f(x)$.

- $x$ — **аргумент** (независимая переменная);
- $y$ — **значение функции** (зависимая переменная);
- буква $f$ — имя функции ($g$, $h$, $\varphi$ — тоже можно).

Ключевое слово — «ровно одно». Если одному $x$ отвечают два разных $y$, это **не функция**.
Например, соответствие $y^2 = x$ функцией не является: при $x=4$ подходят и $y=2$, и $y=-2$.

## Способы задать функцию

1. **Формулой:** $y = 2x + 1$, $y = \sqrt{x}$, $y = \dfrac{1}{x}$.
2. **Графиком:** множество точек $(x;\,f(x))$ на плоскости.
3. **Таблицей:** пары «аргумент — значение».
4. **Словесно:** «каждому числу ставим в соответствие его квадрат».

## Область определения $D(f)$

**Область определения** — множество всех $x$, при которых выражение имеет смысл.
Находят её, выписывая все ограничения. Три главных запрета:

$$\text{деление на ноль} \;\Rightarrow\; \text{знаменатель} \neq 0$$
$$\text{чётный корень } \sqrt[2k]{\ \cdot\ } \;\Rightarrow\; \text{подкоренное} \ge 0$$
$$\text{логарифм } \log_a(\cdot) \;\Rightarrow\; \text{аргумент} > 0,\ a>0,\ a\neq 1$$

Если ограничений несколько — берут их **пересечение** (все должны выполняться одновременно).

**Пример 1.** $f(x) = \dfrac{1}{x-3}$. Запрет один: $x-3\neq 0$, то есть $x\neq 3$.
$$D(f) = (-\infty;\,3)\cup(3;\,+\infty).$$

**Пример 2.** $g(x) = \sqrt{x-2}$. Нужно $x-2\ge 0$, значит $D(g)=[2;\,+\infty)$.

**Пример 3.** $h(x) = \dfrac{\sqrt{x+1}}{x-4}$. Два ограничения:
$x+1\ge 0$ и $x-4\neq 0$. Пересечение: $x\ge -1$ и $x\neq 4$, то есть
$$D(h) = [-1;\,4)\cup(4;\,+\infty).$$

## Множество значений $E(f)$

**Множество значений** — все $y$, которые функция реально принимает.

- $y = x^2$: значения $\ge 0$, значит $E(f)=[0;+\infty)$;
- $y = \dfrac{1}{x}$: любое значение, кроме нуля, $E(f)=(-\infty;0)\cup(0;+\infty)$;
- $y = \sin x$: $E(f)=[-1;1]$.

## График функции

График — «портрет» функции. Проверка «функция ли это» по графику — **тест вертикальной
прямой**: любая вертикаль пересекает график не более чем в одной точке.

> **Засада.** Прежде чем что-то делать с функцией (решать уравнение, строить график,
> искать производную), **найди область определения**. Потерянное ограничение даёт лишние
> или ложные ответы. Область определения — первый шаг почти любой задачи.
`,
  },
  {
    title: 'Наибольшее/наименьшее значение. Чётность и нечётность',
    md: String.raw`# Наибольшее/наименьшее значение. Чётность

## Наибольшее и наименьшее значение

- **Наибольшее** значение функции на множестве — самое большое из всех $f(x)$;
- **Наименьшее** — самое маленькое.

Например, у $y=x^2$ на всей прямой наименьшее значение равно $0$ (достигается в $x=0$),
а наибольшего **нет** — функция растёт неограниченно. У $y=\sin x$: наибольшее $1$,
наименьшее $-1$.

Позже (в теме о производной) появится общий способ искать наибольшее/наименьшее значение
на отрезке — через критические точки и концы отрезка.

## Чётность и нечётность

Пусть область определения **симметрична относительно нуля**: если $x\in D$, то и $-x\in D$.
Только тогда вопрос о чётности имеет смысл.

**Чётная функция:** для всех $x$ выполнено
$$f(-x) = f(x).$$
График симметричен относительно оси $Oy$ (как в зеркале).
Примеры: $y=x^2$, $y=x^4$, $y=\cos x$, $y=|x|$.

**Нечётная функция:** для всех $x$ выполнено
$$f(-x) = -f(x).$$
График симметричен относительно **начала координат** (центральная симметрия).
Примеры: $y=x^3$, $y=x^5$, $y=\sin x$, $y=\dfrac{1}{x}$, $y=\operatorname{tg} x$.

**Функция общего вида:** ни чётная, ни нечётная (например, $y=x^2+x$).

## Как проверить — алгоритм

1. Проверь симметричность $D(f)$. Несимметрична — функция общего вида, дальше не идём.
2. Найди $f(-x)$: подставь $-x$ вместо каждого $x$ и упрости.
3. Сравни с $f(x)$:
   - $f(-x)=f(x)$ — **чётная**;
   - $f(-x)=-f(x)$ — **нечётная**;
   - ни то ни другое — **общего вида**.

**Пример 1.** $f(x)=x^4-x^2$.
$$f(-x)=(-x)^4-(-x)^2 = x^4-x^2 = f(x) \Rightarrow \text{чётная}.$$

**Пример 2.** $f(x)=x^3+ x$.
$$f(-x)=(-x)^3+(-x) = -x^3 - x = -(x^3+x) = -f(x) \Rightarrow \text{нечётная}.$$

**Пример 3.** $f(x)=x^2+x$.
$$f(-x)=x^2 - x \neq f(x),\ \neq -f(x) \Rightarrow \text{общего вида}.$$

## Полезные факты

- Сумма двух чётных — чётная; сумма двух нечётных — нечётная.
- Произведение двух чётных или двух нечётных — **чётная**; чётная $\times$ нечётная — **нечётная**.

> **Засада.** Если область определения несимметрична (например, $D=[0;+\infty)$), функция
> не может быть ни чётной, ни нечётной — проверку даже не начинают.
`,
  },
  {
    title: 'Построение графиков через преобразования (сдвиги, растяжения, отражения)',
    md: String.raw`# Преобразования графиков

Зная график «базовой» функции $y=f(x)$, можно быстро построить график её изменённого
варианта, не считая точки поштучно.

## Сдвиги (параллельный перенос)

$$y = f(x) + a \;\Rightarrow\; \text{сдвиг вверх на } a\ (\text{вниз, если } a<0)$$
$$y = f(x - a) \;\Rightarrow\; \text{сдвиг вправо на } a\ (\text{влево, если } a<0)$$

Запомни правило «наоборот» для аргумента: $f(x-3)$ — сдвиг **вправо** на 3, хотя стоит минус;
$f(x+3)$ — сдвиг влево. По оси $y$ всё «прямо»: $+a$ — вверх.

## Растяжения и сжатия

$$y = k\,f(x)\ (k>0) \;\Rightarrow\; \text{вдоль } Oy:\ \text{растяжение в } k \text{ раз } (k>1)$$
$$y = f(kx)\ (k>0) \;\Rightarrow\; \text{вдоль } Ox:\ \text{сжатие в } k \text{ раз } (k>1)$$

Опять «наоборот» внутри аргумента: $f(2x)$ — **сжатие** по горизонтали в 2 раза,
$f(x/2)$ — растяжение по горизонтали.

## Отражения (симметрии)

$$y = -f(x) \;\Rightarrow\; \text{отражение относительно оси } Ox$$
$$y = f(-x) \;\Rightarrow\; \text{отражение относительно оси } Oy$$

## Модуль

$$y=|f(x)| \;\Rightarrow\; \text{часть графика ниже } Ox \text{ отражают вверх}$$
$$y=f(|x|) \;\Rightarrow\; \text{правую часть графика копируют влево (чётно)}$$

## Порядок преобразований

Для $y = k\,f(x-a)+b$ удобный порядок:
1. **внутренние** (по $x$): сжатие/растяжение по $x$, отражение по $x$, сдвиг по $x$;
2. **внешние** (по $y$): растяжение по $y$, отражение по $y$, сдвиг по $y$.

**Пример 1.** $y=(x-2)^2+1$ — берём параболу $y=x^2$, сдвигаем на $2$ вправо и на $1$ вверх.
Вершина переезжает из $(0;0)$ в $(2;1)$.

**Пример 2.** $y=-2\sqrt{x}$ — график $y=\sqrt{x}$ растянули вдоль $Oy$ в 2 раза и отразили вниз.

**Пример 3.** $y=\cos\!\left(x-\dfrac{\pi}{2}\right)$ — косинусоиду сдвинули вправо на $\dfrac{\pi}{2}$;
получилась синусоида ($\cos(x-\tfrac{\pi}{2})=\sin x$).

> **Совет.** Рисуй по шагам: сначала базовый график, затем каждое преобразование отдельно.
> Контролируй по «характерным точкам» (вершина, нули, точки $(0;\,\cdot)$).
`,
  },
  {
    title: 'Обратная функция',
    md: String.raw`# Обратная функция

## Идея

Функция $g$ называется **обратной** к $f$, если она «отменяет» её действие:
$$g(f(x)) = x \quad\text{и}\quad f(g(x)) = x.$$
Обозначают $g=f^{-1}$. Важно: $f^{-1}$ — это **не** $\dfrac{1}{f}$ и не степень $-1$,
а именно обратная функция.

Смысл: если $f$ переводит $x$ в $y$, то $f^{-1}$ возвращает $y$ обратно в $x$.

## Когда обратная существует

Обратная функция существует, только если $f$ **обратима** — каждому значению $y$ отвечает
единственный $x$. Достаточное условие: функция **строго монотонна** (всюду возрастает или
всюду убывает) на своей области определения.

Проверка по графику — **тест горизонтальной прямой**: любая горизонталь пересекает график
не более одного раза.

## Как найти обратную — алгоритм

1. Записать $y=f(x)$.
2. Выразить $x$ через $y$.
3. Поменять местами обозначения $x$ и $y$ — получаем $y=f^{-1}(x)$.

**Пример 1.** $f(x)=2x+1$.
Из $y=2x+1$ выражаем: $x=\dfrac{y-1}{2}$. Меняем буквы:
$$f^{-1}(x)=\dfrac{x-1}{2}.$$
Проверка: $f^{-1}(f(x))=\dfrac{(2x+1)-1}{2}=x$ — верно.

**Пример 2.** $f(x)=x^3$.
Из $y=x^3$ получаем $x=\sqrt[3]{y}$, значит $f^{-1}(x)=\sqrt[3]{x}$.

**Пример 3.** $f(x)=x^2$ при $x\ge 0$.
На всей прямой $y=x^2$ **не** обратима (не монотонна). Но на $[0;+\infty)$ она возрастает,
и там обратная — это $f^{-1}(x)=\sqrt{x}$.

## Свойства

- $D(f^{-1}) = E(f)$ и $E(f^{-1}) = D(f)$ — области определения и значений меняются местами.
- Графики $f$ и $f^{-1}$ **симметричны относительно прямой** $y=x$.
- Если $f$ возрастает, то и $f^{-1}$ возрастает.

> **Засада.** Для немонотонной функции обратную вводят только на промежутке монотонности,
> обязательно указывая этот промежуток (как с $\sqrt{x}$ при $x\ge 0$).
`,
  },
  {
    title: 'Степенная функция с натуральным и целым показателем',
    md: String.raw`# Степенная функция $y=x^n$

## Натуральный показатель

Поведение зависит от чётности $n$.

**Чётное $n$** ($y=x^2, x^4, x^6,\dots$):
- функция **чётная**, график симметричен относительно $Oy$;
- $D(f)=\mathbb{R}$, $E(f)=[0;+\infty)$;
- убывает на $(-\infty;0]$, возрастает на $[0;+\infty)$;
- проходит через $(0;0)$, $(1;1)$, $(-1;1)$; форма — «парабола» с ветвями вверх.

**Нечётное $n$** ($y=x^3, x^5,\dots$):
- функция **нечётная**, симметрия относительно начала координат;
- $D(f)=\mathbb{R}$, $E(f)=\mathbb{R}$;
- **всюду возрастает**;
- проходит через $(0;0)$, $(1;1)$, $(-1;-1)$.

## Целый отрицательный показатель

$$x^{-n} = \dfrac{1}{x^{n}}, \quad x\neq 0.$$

Для $y=x^{-1}=\dfrac{1}{x}$ — **гипербола**: $D(f)=E(f)=\mathbb{R}\setminus\{0\}$, нечётная,
убывает на каждом из промежутков $(-\infty;0)$ и $(0;+\infty)$. Оси — асимптоты.

Для $y=x^{-2}=\dfrac{1}{x^2}$: чётная, значения $>0$, симметрия относительно $Oy$.

## Нулевой показатель

$$x^0 = 1 \quad (x\neq 0).$$
Выражение $0^0$ не определено.

## Свойства степеней (любые целые $m,n$)

$$x^m\cdot x^n = x^{m+n}, \qquad \dfrac{x^m}{x^n}=x^{m-n}, \qquad (x^m)^n = x^{mn}$$
$$(xy)^n = x^n y^n, \qquad \left(\dfrac{x}{y}\right)^n = \dfrac{x^n}{y^n}, \qquad x^{-n}=\dfrac{1}{x^n}$$

**Пример 1.** $\dfrac{x^5\cdot x^{-2}}{x^{-1}} = x^{5+(-2)-(-1)} = x^{4}.$

**Пример 2.** $\left(\dfrac{2}{x}\right)^{-3} = \left(\dfrac{x}{2}\right)^{3} = \dfrac{x^3}{8}.$

**Пример 3.** $(a^2 b^{-1})^{-2} = a^{-4}b^{2} = \dfrac{b^2}{a^4}.$

> **Засада.** При отрицательном показателе следи за $x\neq 0$. И помни: правило
> $(x^m)^n=x^{mn}$ перемножает показатели, а $x^m\cdot x^n=x^{m+n}$ — складывает; их путают.
`,
  },
  {
    title: 'Корень n-й степени. Арифметический корень',
    md: String.raw`# Корень $n$-й степени

## Определение

**Корнем $n$-й степени** из числа $a$ называют число, $n$-я степень которого равна $a$:
$$\sqrt[n]{a}=b \iff b^n = a.$$
Здесь $n\ge 2$ — натуральное, $n$ — показатель корня, $a$ — подкоренное выражение.

## Чётная и нечётная степень — разница принципиальная

**Нечётный $n$** ($\sqrt[3]{\ },\ \sqrt[5]{\ }$): корень определён для **любого** $a$ и
единственный. Знак корня совпадает со знаком $a$.
$$\sqrt[3]{-8}=-2\ (\text{ведь } (-2)^3=-8), \qquad \sqrt[3]{27}=3.$$

**Чётный $n$** ($\sqrt{\ }=\sqrt[2]{\ },\ \sqrt[4]{\ }$): корень определён **только при $a\ge 0$**.
У положительного $a$ есть два числа с нужной чётной степенью ($\pm b$), поэтому договариваются
брать неотрицательное — **арифметический корень**.

## Арифметический корень

$$\sqrt[n]{a}\ge 0 \quad\text{при } a\ge 0\ (n\ \text{чётное}).$$

Поэтому $\sqrt{16}=4$ (а **не** $\pm 4$). Не путай с уравнением: $x^2=16$ имеет **два** корня
$x=\pm 4$, но само значение $\sqrt{16}$ — одно, равно $4$.

## Ключевое тождество

$$\sqrt[n]{a^{n}} = \begin{cases} a, & n\ \text{нечётное},\\[2pt] |a|, & n\ \text{чётное}. \end{cases}$$

**Примеры.**
$$\sqrt{(-5)^2} = |-5| = 5, \qquad \sqrt[3]{(-5)^3} = -5, \qquad \sqrt{x^2}=|x|.$$

## Значение корня как решение уравнения

$\sqrt[n]{a}$ — это корень уравнения $t^n=a$ (с учётом ограничений выше). Например, $\sqrt{5}$ —
положительное решение $t^2=5$.

## Оценка и сравнение

$$4=\sqrt{16}<\sqrt{20}<\sqrt{25}=5 \Rightarrow \sqrt{20}\in(4;5).$$

> **Засада.** Модуль в $\sqrt{a^2}=|a|$ теряют чаще всего. Для чётного корня одновременно:
> подкоренное $\ge 0$ **и** сам корень $\ge 0$.
`,
  },
  {
    title: 'Свойства корня n-й степени',
    md: String.raw`# Свойства корня $n$-й степени

Для неотрицательных $a,b$ (при чётном $n$; при нечётном — для любых) верны правила:

$$\sqrt[n]{ab}=\sqrt[n]{a}\cdot\sqrt[n]{b}$$
$$\sqrt[n]{\dfrac{a}{b}}=\dfrac{\sqrt[n]{a}}{\sqrt[n]{b}}\ \ (b\neq 0)$$
$$\left(\sqrt[n]{a}\right)^{m}=\sqrt[n]{a^{m}}$$
$$\sqrt[n]{\sqrt[m]{a}}=\sqrt[nm]{a}$$
$$\sqrt[n]{a^{m}}=\sqrt[nk]{a^{mk}}\ \ (\text{показатели корня и степени можно домножать на } k)$$

## Вынесение множителя из-под корня

$$\sqrt{a^2 b}=|a|\sqrt{b}, \qquad \sqrt[3]{a^3 b}=a\sqrt[3]{b}.$$

**Пример 1.** $\sqrt{50}=\sqrt{25\cdot2}=5\sqrt{2}.$
**Пример 2.** $\sqrt{72}=\sqrt{36\cdot2}=6\sqrt{2}.$
**Пример 3.** $\sqrt[3]{54}=\sqrt[3]{27\cdot2}=3\sqrt[3]{2}.$

## Внесение множителя под корень

$$a\sqrt{b}=\sqrt{a^2 b}\ (a\ge 0), \qquad 2\sqrt{3}=\sqrt{4\cdot3}=\sqrt{12}.$$

## Приведение корней к общему показателю

$$\sqrt{2}=\sqrt[6]{2^3}=\sqrt[6]{8}, \qquad \sqrt[3]{3}=\sqrt[6]{3^2}=\sqrt[6]{9}.$$
Теперь их легко сравнить: $\sqrt[6]{8}<\sqrt[6]{9}$, значит $\sqrt{2}<\sqrt[3]{3}$.

## Избавление от иррациональности в знаменателе

$$\dfrac{1}{\sqrt{a}}=\dfrac{\sqrt{a}}{a}, \qquad
\dfrac{1}{\sqrt{a}-\sqrt{b}}=\dfrac{\sqrt{a}+\sqrt{b}}{a-b}\ (\text{домножили на сопряжённое}).$$

**Пример 4.** $\dfrac{1}{\sqrt{3}}=\dfrac{\sqrt{3}}{3}.$
**Пример 5.** $\dfrac{2}{\sqrt{5}-1}=\dfrac{2(\sqrt{5}+1)}{(\sqrt{5})^2-1^2}=\dfrac{2(\sqrt{5}+1)}{4}=\dfrac{\sqrt{5}+1}{2}.$

> **Засада.** Свойства произведения и частного корней работают для **неотрицательных** чисел.
> Нельзя писать $\sqrt{ab}=\sqrt{a}\sqrt{b}$ для отрицательных $a,b$.
`,
  },
  {
    title: 'Степень с рациональным показателем и её свойства',
    md: String.raw`# Степень с рациональным показателем

## Определение

Для основания $a>0$ и дроби $\dfrac{m}{n}$ ($n\ge 2$):
$$a^{\frac{m}{n}}=\sqrt[n]{a^{m}}=\left(\sqrt[n]{a}\right)^{m}.$$

Отрицательный показатель:
$$a^{-\frac{m}{n}}=\dfrac{1}{a^{\frac{m}{n}}}=\dfrac{1}{\sqrt[n]{a^m}}.$$

Смысл: рациональная степень — это компактная запись корня. Именно эта связь нужна дальше
в производных и в показательной/логарифмической функциях.

**Примеры вычислений.**
$$8^{\frac{2}{3}}=\left(\sqrt[3]{8}\right)^2=2^2=4, \qquad
16^{-\frac{1}{2}}=\dfrac{1}{\sqrt{16}}=\dfrac{1}{4},$$
$$27^{\frac{4}{3}}=\left(\sqrt[3]{27}\right)^4=3^4=81, \qquad
32^{0{,}2}=32^{\frac{1}{5}}=\sqrt[5]{32}=2.$$

## Свойства (для $a,b>0$, любых рациональных $p,q$)

$$a^{p}\cdot a^{q}=a^{p+q}, \qquad \dfrac{a^{p}}{a^{q}}=a^{p-q}, \qquad (a^{p})^{q}=a^{pq}$$
$$(ab)^{p}=a^{p}b^{p}, \qquad \left(\dfrac{a}{b}\right)^{p}=\dfrac{a^{p}}{b^{p}}$$

Правила — те же, что для целых степеней, но теперь работают и для дробных показателей.

**Пример 1.** $\sqrt{x}\cdot\sqrt[3]{x}=x^{\frac12}\cdot x^{\frac13}=x^{\frac12+\frac13}=x^{\frac56}=\sqrt[6]{x^5}.$

**Пример 2.** $\dfrac{a^{\frac34}}{a^{\frac14}}=a^{\frac34-\frac14}=a^{\frac12}=\sqrt{a}.$

**Пример 3.** $\left(x^{\frac23}\right)^{6}=x^{4}.$

## Перевод корней в степени и обратно

$$\sqrt[n]{a^m}=a^{\frac{m}{n}}, \qquad \sqrt[3]{a^2}=a^{\frac23}, \qquad \dfrac{1}{\sqrt[4]{a}}=a^{-\frac14}.$$
Это главный приём: сложные корни превращают в степени, применяют свойства, возвращаются к корню.

> **Засада.** Рациональные степени определяют для **основания $a>0$**. Для отрицательных
> оснований (например, $(-8)^{1/3}$) привычные свойства могут не выполняться — такие случаи
> обходят через корень нечётной степени.
`,
  },
  {
    title: 'Иррациональные уравнения',
    md: String.raw`# Иррациональные уравнения

**Иррациональное уравнение** — уравнение, в котором переменная стоит под знаком корня
(или под дробной степенью).

## Основной метод — возведение в степень

Уравнение $\sqrt{f(x)}=g(x)$ решают возведением обеих частей в квадрат. Но возведение в
чётную степень — **не равносильное** преобразование: оно может добавить посторонние корни.
Поэтому переходят к системе:
$$\sqrt{f(x)}=g(x) \iff \begin{cases} f(x)=g(x)^2,\\ g(x)\ge 0. \end{cases}$$
Условие $g(x)\ge 0$ — потому что арифметический корень слева неотрицателен, значит и правая
часть обязана быть $\ge 0$. (Ограничение $f(x)\ge 0$ выполнится автоматически, ведь $f=g^2\ge0$.)

## Обязательная проверка

Самый надёжный способ — подставить каждый найденный корень в **исходное** уравнение и
отбросить те, что не подходят.

## Разобранные примеры

**Пример 1.** $\sqrt{x+2}=x$.
Возводим: $x+2=x^2 \Rightarrow x^2-x-2=0 \Rightarrow x=2$ или $x=-1$.
Проверка: $x=2$: $\sqrt{4}=2$ — верно. $x=-1$: $\sqrt{1}=1\neq -1$ — посторонний.
**Ответ:** $x=2$.

**Пример 2.** $\sqrt{2x+3}=x$.
$2x+3=x^2 \Rightarrow x^2-2x-3=0 \Rightarrow x=3$ или $x=-1$.
$x=3$: $\sqrt{9}=3$ — верно. $x=-1$: правая часть $-1<0$ — не подходит.
**Ответ:** $x=3$.

**Пример 3 (два корня).** $\sqrt{x+7}=\sqrt{2x-1}$.
Оба корня $\ge 0$, можно возвести: $x+7=2x-1 \Rightarrow x=8$.
ОДЗ: $x+7\ge 0$ и $2x-1\ge 0$, то есть $x\ge \tfrac12$. $x=8$ подходит. **Ответ:** $x=8$.

## Приёмы

- Уединить корень (оставить один корень с одной стороны) **перед** возведением.
- Если корней два — иногда возводят дважды.
- Замена $t=\sqrt{\cdot}$ ($t\ge 0$) сводит к обычному уравнению.

> **Засада дня 9.** Возведение в степень плодит посторонние корни. Проверка в конце — не
> формальность, а часть решения. Без неё ответ считается неполным.
`,
  },
  {
    title: 'Радианная мера угла',
    md: String.raw`# Радианная мера угла

## Что такое радиан

**Радиан** — центральный угол, при котором длина дуги равна радиусу окружности.
Это «естественная» мера угла, на которой строится вся тригонометрия и производные
тригонометрических функций.

## Связь градусов и радиан

Полный оборот: $360^\circ = 2\pi$ радиан. Значит $180^\circ=\pi$, откуда
$$1^\circ=\dfrac{\pi}{180}\ \text{рад}, \qquad 1\ \text{рад}=\dfrac{180^\circ}{\pi}\approx 57{,}3^\circ.$$

**Формулы перевода:**
$$\alpha_{\text{рад}}=\alpha_{\text{град}}\cdot\dfrac{\pi}{180}, \qquad
\alpha_{\text{град}}=\alpha_{\text{рад}}\cdot\dfrac{180}{\pi}.$$

## Таблица, которую надо знать наизусть

| Градусы | $0^\circ$ | $30^\circ$ | $45^\circ$ | $60^\circ$ | $90^\circ$ | $180^\circ$ | $270^\circ$ | $360^\circ$ |
|---|---|---|---|---|---|---|---|---|
| Радианы | $0$ | $\dfrac{\pi}{6}$ | $\dfrac{\pi}{4}$ | $\dfrac{\pi}{3}$ | $\dfrac{\pi}{2}$ | $\pi$ | $\dfrac{3\pi}{2}$ | $2\pi$ |

## Примеры перевода

$$120^\circ=120\cdot\dfrac{\pi}{180}=\dfrac{2\pi}{3}, \qquad
\dfrac{5\pi}{6}=\dfrac{5\pi}{6}\cdot\dfrac{180}{\pi}=150^\circ, \qquad
210^\circ=\dfrac{7\pi}{6}.$$

## Длина дуги и площадь сектора

Для угла $\alpha$ **в радианах** и радиуса $R$:
$$\ell=R\alpha \quad (\text{длина дуги}), \qquad S=\dfrac{1}{2}R^2\alpha \quad (\text{площадь сектора}).$$

**Пример.** Дуга радиуса $R=3$ и угла $\dfrac{\pi}{2}$: $\ell=3\cdot\dfrac{\pi}{2}=\dfrac{3\pi}{2}$.

> **Совет.** Заучи $\dfrac{\pi}{6},\dfrac{\pi}{4},\dfrac{\pi}{3},\dfrac{\pi}{2},\pi$ и обратный
> перевод. Формулы длины дуги и сектора работают **только** в радианах.
`,
  },
  {
    title: 'Sin, cos, tg, ctg на единичной окружности. Знаки, чётность',
    md: String.raw`# Тригонометрия на единичной окружности

## Определения через окружность

Возьмём окружность радиуса $1$ с центром в начале координат. Повернём радиус на угол $\alpha$
(против часовой стрелки — положительное направление). Конец радиуса — точка
$P(\cos\alpha;\ \sin\alpha)$.

$$\cos\alpha=x_P\ (\text{абсцисса}), \qquad \sin\alpha=y_P\ (\text{ордината}).$$
$$\operatorname{tg}\alpha=\dfrac{\sin\alpha}{\cos\alpha}\ (\cos\alpha\neq 0), \qquad
\operatorname{ctg}\alpha=\dfrac{\cos\alpha}{\sin\alpha}\ (\sin\alpha\neq 0).$$

Поскольку радиус равен $1$: $-1\le\sin\alpha\le 1$ и $-1\le\cos\alpha\le 1$.

## Знаки по четвертям

| Четверть | углы | $\sin$ | $\cos$ | $\operatorname{tg},\operatorname{ctg}$ |
|---|---|---|---|---|
| I | $0..\tfrac{\pi}{2}$ | $+$ | $+$ | $+$ |
| II | $\tfrac{\pi}{2}..\pi$ | $+$ | $-$ | $-$ |
| III | $\pi..\tfrac{3\pi}{2}$ | $-$ | $-$ | $+$ |
| IV | $\tfrac{3\pi}{2}..2\pi$ | $-$ | $+$ | $-$ |

Мнемоника: синус «смотрит вверх-вниз» (знак = знак $y$), косинус «влево-вправо» (знак = знак $x$).

## Чётность

$$\cos(-\alpha)=\cos\alpha\ (\text{чётная}), \qquad \sin(-\alpha)=-\sin\alpha\ (\text{нечётная}),$$
$$\operatorname{tg}(-\alpha)=-\operatorname{tg}\alpha, \qquad \operatorname{ctg}(-\alpha)=-\operatorname{ctg}\alpha\ (\text{нечётные}).$$

## Таблица опорных значений

| $\alpha$ | $0$ | $\dfrac{\pi}{6}$ | $\dfrac{\pi}{4}$ | $\dfrac{\pi}{3}$ | $\dfrac{\pi}{2}$ |
|---|---|---|---|---|---|
| $\sin$ | $0$ | $\dfrac12$ | $\dfrac{\sqrt2}{2}$ | $\dfrac{\sqrt3}{2}$ | $1$ |
| $\cos$ | $1$ | $\dfrac{\sqrt3}{2}$ | $\dfrac{\sqrt2}{2}$ | $\dfrac12$ | $0$ |
| $\operatorname{tg}$ | $0$ | $\dfrac{1}{\sqrt3}$ | $1$ | $\sqrt3$ | — |

**Пример.** $\cos\dfrac{2\pi}{3}$: угол во II четверти, косинус отрицателен;
$\cos\dfrac{2\pi}{3}=-\cos\dfrac{\pi}{3}=-\dfrac12.$

> **Ядро главы.** Рисуй единичную окружность сам, отмечай точку по углу — тогда знаки и
> значения перестанут требовать зубрёжки, они «читаются» с картинки.
`,
  },
  {
    title: 'Периодичность. Графики sin, cos, tg, ctg',
    md: String.raw`# Периодичность и графики

## Периодическая функция

Функция **периодична**, если существует $T>0$, что $f(x+T)=f(x)$ для всех $x$. Наименьшее
такое $T$ — **период**.

$$\sin(x+2\pi)=\sin x, \qquad \cos(x+2\pi)=\cos x \quad (T=2\pi),$$
$$\operatorname{tg}(x+\pi)=\operatorname{tg}x, \qquad \operatorname{ctg}(x+\pi)=\operatorname{ctg}x \quad (T=\pi).$$

## График $y=\sin x$

- $D=\mathbb{R}$, $E=[-1;1]$, нечётная, период $2\pi$;
- нули: $x=\pi n$; максимум $1$ при $x=\tfrac{\pi}{2}+2\pi n$; минимум $-1$ при $x=-\tfrac{\pi}{2}+2\pi n$;
- проходит через $(0;0)$; форма — «волна» (синусоида).

## График $y=\cos x$

- $D=\mathbb{R}$, $E=[-1;1]$, чётная, период $2\pi$;
- та же волна, сдвинутая: $\cos x=\sin\!\left(x+\tfrac{\pi}{2}\right)$;
- нули: $x=\tfrac{\pi}{2}+\pi n$; максимум $1$ при $x=2\pi n$.

## График $y=\operatorname{tg} x$

- $D:\ x\neq\tfrac{\pi}{2}+\pi n$, $E=\mathbb{R}$, нечётная, период $\pi$;
- вертикальные **асимптоты** $x=\tfrac{\pi}{2}+\pi n$; на каждом промежутке возрастает.

## График $y=\operatorname{ctg} x$

- $D:\ x\neq\pi n$, $E=\mathbb{R}$, нечётная, период $\pi$;
- асимптоты $x=\pi n$; на каждом промежутке убывает.

## Период после преобразования аргумента

$$y=\sin(kx)\ \Rightarrow\ T=\dfrac{2\pi}{|k|}, \qquad y=\operatorname{tg}(kx)\ \Rightarrow\ T=\dfrac{\pi}{|k|}.$$

**Пример 1.** У $y=\cos 3x$ период $T=\dfrac{2\pi}{3}$.
**Пример 2.** У $y=\sin\dfrac{x}{2}$ период $T=\dfrac{2\pi}{1/2}=4\pi$.

## Амплитуда и сдвиг

Для $y=A\sin(kx+\varphi)+b$: $A$ — амплитуда (значения в $[b-|A|;\,b+|A|]$), $b$ — сдвиг по
вертикали, $\varphi$ — фазовый сдвиг.

> **Совет.** Рисуй синусоиды по опорным точкам: нули, максимумы, минимумы за один период,
> затем повтори периодически.
`,
  },
  {
    title: 'Основное тождество. Формулы приведения',
    md: String.raw`# Основное тождество и формулы приведения

## Основное тригонометрическое тождество

$$\sin^2\alpha+\cos^2\alpha=1.$$
Это прямое следствие того, что точка $(\cos\alpha;\sin\alpha)$ лежит на окружности радиуса $1$.

Следствия (делением на $\cos^2$ и на $\sin^2$):
$$1+\operatorname{tg}^2\alpha=\dfrac{1}{\cos^2\alpha}, \qquad 1+\operatorname{ctg}^2\alpha=\dfrac{1}{\sin^2\alpha}.$$
И связь: $\operatorname{tg}\alpha\cdot\operatorname{ctg}\alpha=1$.

**Пример.** Дано $\cos\alpha=\dfrac{3}{5}$, $\alpha$ в IV четверти. Найдём остальные функции.
$$\sin\alpha=-\sqrt{1-\cos^2\alpha}=-\sqrt{1-\tfrac{9}{25}}=-\tfrac{4}{5}\ (\text{в IV синус }<0),$$
$$\operatorname{tg}\alpha=\dfrac{\sin\alpha}{\cos\alpha}=\dfrac{-4/5}{3/5}=-\dfrac{4}{3}.$$

## Формулы приведения

Приводят функции углов $\dfrac{\pi}{2}\pm\alpha$, $\pi\pm\alpha$, $\dfrac{3\pi}{2}\pm\alpha$,
$2\pi\pm\alpha$ к функциям острого угла $\alpha$. Работают по правилу из двух шагов.

**Шаг 1 — меняется ли функция?**
- углы с $\dfrac{\pi}{2}$ и $\dfrac{3\pi}{2}$ (точки на вертикальной оси): функция **меняется**
  на кофункцию — $\sin\leftrightarrow\cos$, $\operatorname{tg}\leftrightarrow\operatorname{ctg}$;
- углы с $\pi$ и $2\pi$ (горизонтальная ось): функция **сохраняется**.

**Шаг 2 — знак.** Считаем $\alpha$ острым, смотрим, в какой четверти оказывается исходный угол,
и ставим знак **исходной** функции в этой четверти.

$$\sin\!\left(\dfrac{\pi}{2}-\alpha\right)=\cos\alpha, \qquad \cos\!\left(\dfrac{\pi}{2}+\alpha\right)=-\sin\alpha,$$
$$\cos(\pi-\alpha)=-\cos\alpha, \qquad \sin(\pi+\alpha)=-\sin\alpha, \qquad \operatorname{tg}(\pi+\alpha)=\operatorname{tg}\alpha.$$

**Пример.** $\cos\!\left(\dfrac{3\pi}{2}+\alpha\right)$: с $\dfrac{3\pi}{2}$ функция меняется
($\cos\to\sin$); угол $\dfrac{3\pi}{2}+\alpha$ (при остром $\alpha$) — IV четверть, там косинус $>0$.
Итог: $\cos\!\left(\dfrac{3\pi}{2}+\alpha\right)=\sin\alpha$.

> **«Лошадиное правило».** «Кивок головой» по горизонтали ($\pi,2\pi$) — «да, функция та же»;
> «кивок» по вертикали ($\tfrac{\pi}{2},\tfrac{3\pi}{2}$) — «нет, меняем на кофункцию». Знак — по четверти.
`,
  },
  {
    title: 'Формулы сложения',
    md: String.raw`# Формулы сложения

## Основные формулы

$$\sin(\alpha\pm\beta)=\sin\alpha\cos\beta\pm\cos\alpha\sin\beta$$
$$\cos(\alpha\pm\beta)=\cos\alpha\cos\beta\mp\sin\alpha\sin\beta$$
$$\operatorname{tg}(\alpha\pm\beta)=\dfrac{\operatorname{tg}\alpha\pm\operatorname{tg}\beta}{1\mp\operatorname{tg}\alpha\,\operatorname{tg}\beta}$$

Обрати внимание: у косинуса знаки «перевёрнуты» относительно левой части (в $\cos(\alpha+\beta)$
стоит минус), и в знаменателе тангенса тоже противоположный знак.

## Зачем они нужны

Это «материнские» формулы: из них выводятся двойной угол ($\beta=\alpha$), формулы приведения,
переход суммы в произведение. Практически вся дальнейшая тригонометрия опирается на них.

## Разобранные примеры

**Пример 1 (точное значение).**
$$\cos 75^\circ=\cos(45^\circ+30^\circ)=\cos45^\circ\cos30^\circ-\sin45^\circ\sin30^\circ
=\dfrac{\sqrt2}{2}\cdot\dfrac{\sqrt3}{2}-\dfrac{\sqrt2}{2}\cdot\dfrac12=\dfrac{\sqrt6-\sqrt2}{4}.$$

**Пример 2.**
$$\sin 15^\circ=\sin(45^\circ-30^\circ)=\sin45^\circ\cos30^\circ-\cos45^\circ\sin30^\circ=\dfrac{\sqrt6-\sqrt2}{4}.$$

**Пример 3 (упрощение).**
$$\sin(x+y)+\sin(x-y)=2\sin x\cos y.$$
(Раскрыли обе скобки — слагаемые с $\cos x\sin y$ сократились.)

**Пример 4 (тангенс).**
$$\operatorname{tg}(45^\circ+\alpha)=\dfrac{1+\operatorname{tg}\alpha}{1-\operatorname{tg}\alpha}.$$

> **Наизусть.** Эти три формулы — фундамент главы 4. Самая частая ошибка — знак в косинусе;
> проверяй его отдельно.
`,
  },
  {
    title: 'Формулы двойного аргумента, суммы ↔ произведения',
    md: String.raw`# Двойной аргумент. Сумма ↔ произведение

## Формулы двойного аргумента

$$\sin 2\alpha=2\sin\alpha\cos\alpha$$
$$\cos 2\alpha=\cos^2\alpha-\sin^2\alpha=1-2\sin^2\alpha=2\cos^2\alpha-1$$
$$\operatorname{tg} 2\alpha=\dfrac{2\operatorname{tg}\alpha}{1-\operatorname{tg}^2\alpha}$$

Все три легко вывести из формул сложения, положив $\beta=\alpha$.

## Формулы понижения степени

Из трёх видов $\cos 2\alpha$:
$$\sin^2\alpha=\dfrac{1-\cos 2\alpha}{2}, \qquad \cos^2\alpha=\dfrac{1+\cos 2\alpha}{2}.$$
Незаменимы при интегрировании и упрощении квадратов синуса/косинуса.

## Сумма в произведение

$$\sin\alpha+\sin\beta=2\sin\dfrac{\alpha+\beta}{2}\cos\dfrac{\alpha-\beta}{2}$$
$$\sin\alpha-\sin\beta=2\cos\dfrac{\alpha+\beta}{2}\sin\dfrac{\alpha-\beta}{2}$$
$$\cos\alpha+\cos\beta=2\cos\dfrac{\alpha+\beta}{2}\cos\dfrac{\alpha-\beta}{2}$$
$$\cos\alpha-\cos\beta=-2\sin\dfrac{\alpha+\beta}{2}\sin\dfrac{\alpha-\beta}{2}$$

## Произведение в сумму

$$\sin\alpha\cos\beta=\tfrac12\big(\sin(\alpha+\beta)+\sin(\alpha-\beta)\big)$$
$$\cos\alpha\cos\beta=\tfrac12\big(\cos(\alpha-\beta)+\cos(\alpha+\beta)\big)$$
$$\sin\alpha\sin\beta=\tfrac12\big(\cos(\alpha-\beta)-\cos(\alpha+\beta)\big)$$

## Примеры

**Пример 1.** $\sin 3x+\sin x=2\sin 2x\cos x.$

**Пример 2.** Если $\sin\alpha=\dfrac35$, $\alpha$ острый, то $\cos\alpha=\dfrac45$ и
$$\sin 2\alpha=2\cdot\dfrac35\cdot\dfrac45=\dfrac{24}{25}, \qquad \cos 2\alpha=1-2\cdot\dfrac{9}{25}=\dfrac{7}{25}.$$

**Пример 3 (упрощение уравнения).** $\cos x+\cos 3x=2\cos 2x\cos x$ — превратили сумму в
произведение, что упрощает решение.

> **Совет.** Не зубри вслепую: двойной угол выводится из сложения, а сумма↔произведение — из
> тех же формул. Понимание вывода надёжнее памяти.
`,
  },
  {
    title: 'Уравнение cos x = a. Арккосинус',
    md: String.raw`# Уравнение $\cos x=a$

## Арккосинус

$$\arccos a=x \iff \cos x=a,\quad x\in[0;\pi],\quad a\in[-1;1].$$
То есть $\arccos a$ — это тот единственный угол из $[0;\pi]$, косинус которого равен $a$.

Свойства: $\arccos(-a)=\pi-\arccos a$. Значения:
$$\arccos 1=0,\quad \arccos\dfrac{\sqrt3}{2}=\dfrac{\pi}{6},\quad \arccos\dfrac{\sqrt2}{2}=\dfrac{\pi}{4},\quad \arccos\dfrac12=\dfrac{\pi}{3},\quad \arccos 0=\dfrac{\pi}{2},\quad \arccos(-1)=\pi.$$

## Общая формула корней

При $|a|\le 1$:
$$\cos x=a \;\Rightarrow\; x=\pm\arccos a+2\pi n,\quad n\in\mathbb{Z}.$$
Знак $\pm$ обязателен: на окружности есть **два** угла с данным косинусом, симметричные
относительно оси $Ox$. При $|a|>1$ решений нет.

## Частные случаи (проще помнить отдельно)

$$\cos x=1 \Rightarrow x=2\pi n; \qquad \cos x=-1 \Rightarrow x=\pi+2\pi n; \qquad \cos x=0 \Rightarrow x=\dfrac{\pi}{2}+\pi n.$$

## Примеры

**Пример 1.** $\cos x=\dfrac12 \Rightarrow x=\pm\dfrac{\pi}{3}+2\pi n.$

**Пример 2.** $\cos x=-\dfrac{\sqrt2}{2}$. Здесь $\arccos\!\left(-\tfrac{\sqrt2}{2}\right)=\pi-\dfrac{\pi}{4}=\dfrac{3\pi}{4}$,
значит $x=\pm\dfrac{3\pi}{4}+2\pi n.$

**Пример 3.** $\cos 2x=1 \Rightarrow 2x=2\pi n \Rightarrow x=\pi n.$

> **Засада.** Знак $\pm$ нельзя терять. И для «хитрого» аргумента (как $2x$) сначала находят
> весь аргумент, потом делят — с учётом всех серий $n$.
`,
  },
  {
    title: 'Уравнение sin x = a. Арксинус',
    md: String.raw`# Уравнение $\sin x=a$

## Арксинус

$$\arcsin a=x \iff \sin x=a,\quad x\in\left[-\dfrac{\pi}{2};\dfrac{\pi}{2}\right],\quad a\in[-1;1].$$
$\arcsin a$ — единственный угол из $\left[-\tfrac{\pi}{2};\tfrac{\pi}{2}\right]$ с синусом $a$.

Свойство: $\arcsin(-a)=-\arcsin a$ (нечётность). Значения:
$$\arcsin 0=0,\ \arcsin\dfrac12=\dfrac{\pi}{6},\ \arcsin\dfrac{\sqrt2}{2}=\dfrac{\pi}{4},\ \arcsin\dfrac{\sqrt3}{2}=\dfrac{\pi}{3},\ \arcsin 1=\dfrac{\pi}{2}.$$

## Общая формула корней

При $|a|\le 1$:
$$\sin x=a \;\Rightarrow\; x=(-1)^n\arcsin a+\pi n,\quad n\in\mathbb{Z}.$$
Множитель $(-1)^n$ «переключает» между двумя сериями решений. При $|a|>1$ решений нет.

## Частные случаи

$$\sin x=1 \Rightarrow x=\dfrac{\pi}{2}+2\pi n; \qquad \sin x=-1 \Rightarrow x=-\dfrac{\pi}{2}+2\pi n; \qquad \sin x=0 \Rightarrow x=\pi n.$$

## Примеры

**Пример 1.** $\sin x=\dfrac{\sqrt2}{2} \Rightarrow x=(-1)^n\dfrac{\pi}{4}+\pi n.$

**Пример 2.** $\sin x=-\dfrac12$. $\arcsin\!\left(-\tfrac12\right)=-\dfrac{\pi}{6}$,
значит $x=(-1)^n\!\left(-\dfrac{\pi}{6}\right)+\pi n=(-1)^{n+1}\dfrac{\pi}{6}+\pi n.$

**Пример 3.** $\sin x=1 \Rightarrow x=\dfrac{\pi}{2}+2\pi n$ (удобнее частная формула, чем общая).

> **Засада.** Множитель $(-1)^n$ теряют чаще всего. В частных случаях ($0,\pm 1$) пользуйся
> отдельными формулами — общая там избыточна и путает.
`,
  },
  {
    title: 'Уравнение tg x = a. Арктангенс',
    md: String.raw`# Уравнение $\operatorname{tg} x=a$

## Арктангенс

$$\operatorname{arctg} a=x \iff \operatorname{tg} x=a,\quad x\in\left(-\dfrac{\pi}{2};\dfrac{\pi}{2}\right),\quad a\in\mathbb{R}.$$
$\operatorname{arctg} a$ — единственный угол из открытого промежутка $\left(-\tfrac{\pi}{2};\tfrac{\pi}{2}\right)$
с тангенсом $a$. Тангенс принимает **любые** значения, поэтому $a$ — любое число.

Свойство: $\operatorname{arctg}(-a)=-\operatorname{arctg} a$. Значения:
$$\operatorname{arctg} 0=0,\quad \operatorname{arctg}\dfrac{1}{\sqrt3}=\dfrac{\pi}{6},\quad \operatorname{arctg} 1=\dfrac{\pi}{4},\quad \operatorname{arctg}\sqrt3=\dfrac{\pi}{3}.$$

## Общая формула корней

Для **любого** $a$:
$$\operatorname{tg} x=a \;\Rightarrow\; x=\operatorname{arctg} a+\pi n,\quad n\in\mathbb{Z}.$$
Период тангенса равен $\pi$, поэтому шаг серии — $\pi n$ (не $2\pi n$), и знака $\pm$ нет —
на периоде $\pi$ ровно одно решение.

**Область.** Не забывай ограничение $x\neq\dfrac{\pi}{2}+\pi k$ (там тангенс не определён);
в уравнениях, где тангенс возник после деления, это условие проверяют отдельно.

## Примеры

**Пример 1.** $\operatorname{tg} x=1 \Rightarrow x=\dfrac{\pi}{4}+\pi n.$

**Пример 2.** $\operatorname{tg} x=-\sqrt3 \Rightarrow x=-\dfrac{\pi}{3}+\pi n.$

**Пример 3.** $\operatorname{tg} 3x=1 \Rightarrow 3x=\dfrac{\pi}{4}+\pi n \Rightarrow x=\dfrac{\pi}{12}+\dfrac{\pi n}{3}.$

> **Засада.** Шаг именно $\pi n$. И для аргумента вида $3x$ делят на 3 **и правую часть, и $\pi n$**
> — иначе теряют часть решений.
`,
  },
  {
    title: 'Тригонометрические уравнения (замена, разложение, однородные)',
    md: String.raw`# Методы решения тригонометрических уравнений

Общая идея: свести сложное уравнение к **простейшим** ($\sin x=a$, $\cos x=a$, $\operatorname{tg} x=a$),
формулы корней которых известны.

## 1. Замена переменной

Если уравнение — многочлен относительно одной функции, вводят новую переменную.
**Пример.** $2\sin^2 x-3\sin x+1=0$. Пусть $t=\sin x$, $t\in[-1;1]$:
$$2t^2-3t+1=0 \Rightarrow t=1\ \text{или}\ t=\tfrac12.$$
Значит $\sin x=1$ ($x=\tfrac{\pi}{2}+2\pi n$) или $\sin x=\tfrac12$ ($x=(-1)^n\tfrac{\pi}{6}+\pi n$).

## 2. Разложение на множители

Выносят общий множитель, приравнивают каждый к нулю (произведение равно нулю, когда хоть один
множитель ноль).
**Пример.** $\sin x\cos x-\sin x=0 \Rightarrow \sin x(\cos x-1)=0$.
Отсюда $\sin x=0$ ($x=\pi n$) или $\cos x=1$ ($x=2\pi n$). Объединяя: $x=\pi n$.

## 3. Однородные уравнения

**Первой степени** $a\sin x+b\cos x=0$: делят на $\cos x\neq 0$:
$$a\operatorname{tg} x+b=0 \Rightarrow \operatorname{tg} x=-\dfrac{b}{a}.$$
**Второй степени** $a\sin^2 x+b\sin x\cos x+c\cos^2 x=0$: делят на $\cos^2 x$, получают
квадратное уравнение относительно $\operatorname{tg} x$.
**Пример.** $\sin^2 x-3\sin x\cos x+2\cos^2 x=0$. Делим на $\cos^2 x$:
$\operatorname{tg}^2 x-3\operatorname{tg} x+2=0 \Rightarrow \operatorname{tg} x=1$ или $\operatorname{tg} x=2$.

## 4. Сведение к одной функции

Через основное тождество $\sin^2 x=1-\cos^2 x$, формулы приведения и двойного угла всё выражают
через одну функцию.
**Пример.** $\cos 2x+\sin x=0 \Rightarrow (1-2\sin^2 x)+\sin x=0 \Rightarrow 2\sin^2 x-\sin x-1=0$ —
далее замена $t=\sin x$.

## 5. Метод вспомогательного угла

$a\sin x+b\cos x=R\sin(x+\varphi)$, где $R=\sqrt{a^2+b^2}$. Полезно для уравнений вида
$a\sin x+b\cos x=c$.

> **Совет.** Сначала ищи разложение на множители — это чаще всего короче. И следи за областью:
> деление на $\cos x$ или $\sin x$ требует, чтобы делитель был $\neq 0$ (проверяй отдельно).
`,
  },
  {
    title: 'Предел функции, непрерывность',
    md: String.raw`# Предел и непрерывность

## Предел функции

Запись $\lim\limits_{x\to a}f(x)=L$ означает: когда $x$ неограниченно приближается к $a$
(не обязательно достигая его), значения $f(x)$ становятся сколь угодно близки к числу $L$.

**Простые пределы (подстановкой).** Если функция «хорошая» в точке $a$, предел равен значению:
$$\lim_{x\to 2}(3x+1)=7, \qquad \lim_{x\to 0}\cos x=1.$$

## Свойства пределов

Если пределы $\lim f$ и $\lim g$ существуют:
$$\lim(f\pm g)=\lim f\pm\lim g, \quad \lim(fg)=\lim f\cdot\lim g, \quad \lim\dfrac{f}{g}=\dfrac{\lim f}{\lim g}\ (\lim g\neq 0).$$

## Замечательные пределы

$$\lim_{x\to 0}\dfrac{\sin x}{x}=1, \qquad \lim_{x\to 0}\dfrac{1-\cos x}{x^2}=\dfrac12.$$

## Раскрытие неопределённости $\dfrac{0}{0}$

Часто помогает разложение на множители и сокращение.
$$\lim_{x\to 3}\dfrac{x^2-9}{x-3}=\lim_{x\to 3}\dfrac{(x-3)(x+3)}{x-3}=\lim_{x\to 3}(x+3)=6.$$
$$\lim_{x\to 1}\dfrac{x^2-1}{x-1}=\lim_{x\to 1}(x+1)=2.$$

## Непрерывность

Функция $f$ **непрерывна** в точке $a$, если
$$\lim_{x\to a}f(x)=f(a).$$
То есть предел существует и совпадает со значением. Непрерывны всюду: многочлены, $\sin x$,
$\cos x$, показательная $a^x$; дробь — там, где знаменатель $\neq 0$.

График непрерывной функции рисуется «не отрывая карандаш от бумаги». Точки разрыва — там, где
это правило нарушается (скачок, разрыв, бесконечность).

> **Совет дня 22.** Здесь важнее идея, чем техника: понять смысл «приближения» и непрерывности.
> На этом фундаменте строится определение производной.
`,
  },
  {
    title: 'Определение производной. Геометрический (касательная) и физический (скорость) смысл',
    md: String.raw`# Производная: определение и смысл

## Приращения

$\Delta x$ — приращение аргумента, $\Delta y=f(x+\Delta x)-f(x)$ — приращение функции.
Отношение $\dfrac{\Delta y}{\Delta x}$ — **средняя скорость изменения** функции на участке.

## Определение производной

**Производная** функции $f$ в точке $x$ — предел отношения приращения функции к приращению
аргумента, когда приращение аргумента стремится к нулю:
$$f'(x)=\lim_{\Delta x\to 0}\dfrac{f(x+\Delta x)-f(x)}{\Delta x}.$$
Если предел существует (и конечен), функцию называют **дифференцируемой** в точке.
Обозначения: $f'(x)$, $y'$, $\dfrac{dy}{dx}$.

## Геометрический смысл

$f'(x_0)$ — **угловой коэффициент касательной** к графику в точке с абсциссой $x_0$:
$$k_{\text{кас}}=f'(x_0)=\operatorname{tg}\varphi,$$
где $\varphi$ — угол наклона касательной к оси $Ox$. Секущая через две близкие точки в пределе
превращается в касательную — отсюда и определение.

## Физический смысл

Если $s(t)$ — координата (путь) в момент $t$, то производная — **мгновенная скорость**:
$$v(t)=s'(t).$$
Аналогично ускорение $a(t)=v'(t)$. Вообще производная — это **скорость изменения** любой величины.

## Пример (по определению)

Для $f(x)=x^2$:
$$f'(x)=\lim_{\Delta x\to 0}\dfrac{(x+\Delta x)^2-x^2}{\Delta x}
=\lim_{\Delta x\to 0}\dfrac{2x\Delta x+\Delta x^2}{\Delta x}=\lim_{\Delta x\to 0}(2x+\Delta x)=2x.$$

Для $f(x)=x^3$ аналогично получится $f'(x)=3x^2$. Отсюда угадывается общее правило $(x^n)'=nx^{n-1}$,
которое дальше оформят в таблицу.

> **Смысл.** Производная отвечает на вопрос «как быстро меняется функция в данной точке» —
> и геометрически (наклон касательной), и физически (скорость).
`,
  },
  {
    title: 'Таблица производных. Правила дифференцирования (сумма, произведение, частное)',
    md: String.raw`# Таблица производных и правила

## Таблица (заучить наизусть)

$$C'=0, \qquad (x)'=1, \qquad (x^n)'=n x^{n-1}, \qquad (\sqrt{x})'=\dfrac{1}{2\sqrt{x}}, \qquad \left(\dfrac{1}{x}\right)'=-\dfrac{1}{x^2}$$
$$(\sin x)'=\cos x, \qquad (\cos x)'=-\sin x$$
$$(\operatorname{tg} x)'=\dfrac{1}{\cos^2 x}, \qquad (\operatorname{ctg} x)'=-\dfrac{1}{\sin^2 x}$$
$$(e^x)'=e^x, \qquad (a^x)'=a^x\ln a, \qquad (\ln x)'=\dfrac{1}{x}, \qquad (\log_a x)'=\dfrac{1}{x\ln a}$$

## Правила дифференцирования

$$(u\pm v)'=u'\pm v' \qquad (\text{производная суммы})$$
$$(Cu)'=C\,u' \qquad (\text{постоянный множитель выносится})$$
$$(uv)'=u'v+uv' \qquad (\text{производная произведения})$$
$$\left(\dfrac{u}{v}\right)'=\dfrac{u'v-uv'}{v^2} \qquad (\text{производная частного})$$

## Разобранные примеры

**Пример 1 (сумма и степень).** $\big(3x^4-5x^2+7\big)'=12x^3-10x.$

**Пример 2 (корень как степень).** $\big(\sqrt{x}\big)'=\big(x^{1/2}\big)'=\dfrac12 x^{-1/2}=\dfrac{1}{2\sqrt{x}}.$

**Пример 3 (произведение).** $\big(x^3\sin x\big)'=3x^2\sin x+x^3\cos x.$

**Пример 4 (частное).**
$$\left(\dfrac{x}{\cos x}\right)'=\dfrac{1\cdot\cos x-x\cdot(-\sin x)}{\cos^2 x}=\dfrac{\cos x+x\sin x}{\cos^2 x}.$$

**Пример 5 (частное с многочленами).**
$$\left(\dfrac{x^2+1}{x-1}\right)'=\dfrac{2x(x-1)-(x^2+1)\cdot 1}{(x-1)^2}=\dfrac{x^2-2x-1}{(x-1)^2}.$$

> **Заучить.** Знаешь таблицу и четыре правила — считаешь производную любой элементарной функции.
> Дальше это чистая техника; ошибаются обычно в знаках ($\cos'=-\sin$) и в правиле частного.
`,
  },
  {
    title: 'Производная сложной функции',
    md: String.raw`# Производная сложной функции (цепное правило)

## Что такое сложная функция

Сложная функция — «функция от функции»: $y=f(g(x))$, где $g$ — внутренняя, $f$ — внешняя.
Например, в $y=\sin(x^2)$ внешняя — синус, внутренняя — $x^2$.

## Цепное правило

$$y=f(g(x)) \;\Rightarrow\; y'=f'(g(x))\cdot g'(x).$$
Словами: производная внешней функции (взятая во внутреннем аргументе) **умножается** на
производную внутренней. Дифференцируем «снаружи внутрь».

## Частые готовые случаи

$$\big(\sin(kx)\big)'=k\cos(kx), \qquad \big(\cos(kx)\big)'=-k\sin(kx)$$
$$\big((ax+b)^n\big)'=n(ax+b)^{n-1}\cdot a, \qquad \big(\sqrt{u}\big)'=\dfrac{u'}{2\sqrt{u}}$$
$$\big(e^{u}\big)'=e^{u}\cdot u', \qquad \big(\ln u\big)'=\dfrac{u'}{u}$$

## Разобранные примеры

**Пример 1.** $\big(\sin 3x\big)'=\cos 3x\cdot 3=3\cos 3x.$

**Пример 2.** $\big((2x+1)^5\big)'=5(2x+1)^4\cdot 2=10(2x+1)^4.$

**Пример 3.** $\big(\sqrt{x^2+1}\big)'=\dfrac{(x^2+1)'}{2\sqrt{x^2+1}}=\dfrac{2x}{2\sqrt{x^2+1}}=\dfrac{x}{\sqrt{x^2+1}}.$

**Пример 4.** $\big(\cos^2 x\big)'=\big((\cos x)^2\big)'=2\cos x\cdot(-\sin x)=-\sin 2x.$

**Пример 5.** $\big(e^{3x}\big)'=3e^{3x}, \qquad \big(\ln(5x-2)\big)'=\dfrac{5}{5x-2}.$

## Несколько вложений

$$y=f(g(h(x))) \;\Rightarrow\; y'=f'\cdot g'\cdot h' \quad (\text{каждая производная — в своём аргументе}).$$
**Пример.** $\big(\sin^2(3x)\big)'=2\sin(3x)\cdot\cos(3x)\cdot 3=3\sin 6x.$

> **Совет.** Разбирай функцию слоями снаружи внутрь и не забывай домножать на производную
> внутренней части — потерянный множитель $g'(x)$ — самая частая ошибка.
`,
  },
  {
    title: 'Уравнение касательной к графику функции',
    md: String.raw`# Уравнение касательной

## Формула

Касательная к графику $y=f(x)$ в точке с абсциссой $x_0$:
$$y=f(x_0)+f'(x_0)\,(x-x_0).$$
Здесь $f(x_0)$ — ордината точки касания, $f'(x_0)$ — угловой коэффициент (наклон).

## Алгоритм

1. Найти $f(x_0)$ — ордината точки касания.
2. Найти $f'(x)$ и подставить $x_0$ — это угловой коэффициент $k=f'(x_0)$.
3. Подставить всё в формулу и упростить.

## Разобранные примеры

**Пример 1.** $f(x)=x^2$, $x_0=1$.
$f(1)=1$; $f'(x)=2x$, $f'(1)=2$. Тогда
$$y=1+2(x-1)=2x-1.$$

**Пример 2.** $f(x)=\dfrac{1}{x}$, $x_0=2$.
$f(2)=\dfrac12$; $f'(x)=-\dfrac{1}{x^2}$, $f'(2)=-\dfrac14$. Касательная:
$$y=\dfrac12-\dfrac14(x-2)=-\dfrac14 x+1.$$

**Пример 3 (наклон и угол).** Если $f'(x_0)=1$, то касательная наклонена под углом $45^\circ$
($\operatorname{tg}\varphi=1$).

## Полезные факты

- Касательная **горизонтальна** ($y=\text{const}$), если $f'(x_0)=0$.
- Угол наклона: $\operatorname{tg}\varphi=f'(x_0)$.
- Если известен угловой коэффициент $k$, точку касания ищут из уравнения $f'(x_0)=k$.

> **Совет.** Формула одна, но чаще всего путают ординату точки касания $f(x_0)$ (берут $x_0$
> вместо $f(x_0)$) и забывают подставить $x_0$ в производную. Прорешай много однотипных.
`,
  },
  {
    title: 'Возрастание и убывание функции. Критические точки',
    md: String.raw`# Монотонность и критические точки

## Признак монотонности по производной

На промежутке, где производная сохраняет знак:
$$f'(x)>0\ \text{на промежутке} \;\Rightarrow\; f\ \text{возрастает},$$
$$f'(x)<0\ \text{на промежутке} \;\Rightarrow\; f\ \text{убывает}.$$
Геометрически: положительный наклон касательной — график идёт вверх; отрицательный — вниз.

## Критические точки

**Критические точки** — точки области определения, в которых $f'(x)=0$ либо $f'(x)$ не
существует. Именно в них функция может сменить возрастание на убывание или наоборот.
Точки, где $f'(x)=0$, называют ещё **стационарными**.

## Алгоритм исследования на монотонность

1. Найти область определения $D(f)$.
2. Найти $f'(x)$.
3. Решить $f'(x)=0$ и найти точки, где $f'$ не существует — это критические точки.
4. Отметить их на числовой прямой, разбить $D(f)$ на промежутки.
5. На каждом промежутке определить знак $f'$ (подстановкой пробной точки).
6. Выписать промежутки возрастания ($f'>0$) и убывания ($f'<0$).

## Разобранный пример

$f(x)=x^3-3x$. Тогда
$$f'(x)=3x^2-3=3(x-1)(x+1).$$
Критические точки: $x=-1$ и $x=1$. Знаки $f'$:
- на $(-\infty;-1)$: $f'>0$ — возрастает;
- на $(-1;1)$: $f'<0$ — убывает;
- на $(1;+\infty)$: $f'>0$ — возрастает.

**Второй пример.** $f(x)=\dfrac{x}{x^2+1}$. $f'(x)=\dfrac{1-x^2}{(x^2+1)^2}$; знак определяется
числителем $1-x^2$: возрастает на $(-1;1)$, убывает вне него.

> **Начало исследования функции.** Знак производной между критическими точками управляет
> ростом и убыванием — это ядро всего дальнейшего исследования (дни 27–30).
`,
  },
  {
    title: 'Точки экстремума: максимумы и минимумы',
    md: String.raw`# Точки экстремума

## Определения

- **Точка максимума** $x_0$: в её окрестности слева функция растёт, справа убывает — «вершина».
- **Точка минимума** $x_0$: слева убывает, справа растёт — «впадина».

Значение функции в такой точке называют **максимумом** (или минимумом) функции. Общее слово —
**экстремум**. Экстремумы бывают только в **критических точках**.

## Признак экстремума по первой производной

Пусть $x_0$ — критическая точка. Смотрим, как $f'$ меняет знак при переходе через $x_0$:
$$f':\ +\to- \;\Rightarrow\; \text{максимум в } x_0,$$
$$f':\ -\to+ \;\Rightarrow\; \text{минимум в } x_0,$$
$$f'\ \text{не меняет знак} \;\Rightarrow\; \text{экстремума нет}.$$

## Признак по второй производной (короткий)

Если $f'(x_0)=0$ и $f''(x_0)\neq 0$:
$$f''(x_0)>0 \Rightarrow \text{минимум}, \qquad f''(x_0)<0 \Rightarrow \text{максимум}.$$

## Алгоритм

1. Найти $f'(x)$ и критические точки.
2. Определить знаки $f'$ слева и справа от каждой.
3. По смене знака выписать максимумы и минимумы.
4. Вычислить значения функции в точках экстремума.

## Разобранный пример

$f(x)=x^3-3x$, $f'(x)=3(x-1)(x+1)$.
- в $x=-1$: $f'$ меняет $+\to-$ — **максимум**, $f(-1)=(-1)^3-3(-1)=2$;
- в $x=1$: $f'$ меняет $-\to+$ — **минимум**, $f(1)=1-3=-2$.

**Контрпример.** $f(x)=x^3$: $f'(x)=3x^2$, в нуле $f'=0$, но знак не меняется ($+\to+$) —
экстремума нет (это точка перегиба).

> **Важно.** Экстремум ищут только среди критических точек, но **не каждая** критическая точка
> — экстремум. Всегда проверяй смену знака производной.
`,
  },
  {
    title: 'Наибольшее и наименьшее значение функции на отрезке',
    md: String.raw`# Наибольшее и наименьшее значение на отрезке

## Теорема (Вейерштрасса, на школьном языке)

Непрерывная на **отрезке** $[a;b]$ функция обязательно достигает и наибольшего, и наименьшего
значения. Достигаются они либо во **внутренних критических точках**, либо на **концах** отрезка.

## Алгоритм

1. Найти $f'(x)$ и критические точки, лежащие **строго внутри** $[a;b]$.
2. Вычислить значения $f$ в этих критических точках **и на концах** $a$ и $b$.
3. Из всех полученных чисел выбрать наибольшее и наименьшее.

Строить полный график и исследовать монотонность **не обязательно** — достаточно сравнить
конечный список значений.

## Разобранный пример 1

$f(x)=x^3-3x$ на отрезке $[0;2]$.
$f'(x)=3x^2-3=0 \Rightarrow x=\pm 1$; внутрь $[0;2]$ попадает только $x=1$.
Считаем значения:
$$f(0)=0, \qquad f(1)=1-3=-2, \qquad f(2)=8-6=2.$$
$$\max_{[0;2]}f=2\ (\text{в } x=2), \qquad \min_{[0;2]}f=-2\ (\text{в } x=1).$$

## Разобранный пример 2

$f(x)=x+\dfrac{4}{x}$ на $[1;4]$.
$f'(x)=1-\dfrac{4}{x^2}=0 \Rightarrow x^2=4 \Rightarrow x=2$ (внутри отрезка).
$$f(1)=5, \qquad f(2)=2+2=4, \qquad f(4)=4+1=5.$$
$$\min=4\ (x=2), \qquad \max=5\ (x=1\ \text{и}\ x=4).$$

## Замечание про интервал

На **открытом** промежутке или на всей прямой наибольшего/наименьшего значения может не быть —
теорема гарантирует их только для отрезка $[a;b]$.

> **Классика контрольных и ЕГЭ.** Не забывай считать значения на концах отрезка — часто именно
> там прячется наибольшее или наименьшее. Это финальная задача всего курса (день 30 — полное
> исследование функции собирает вместе всё: ОДЗ, чётность, производную, экстремумы и график).
`,
  },
];
